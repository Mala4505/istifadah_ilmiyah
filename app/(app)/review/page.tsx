import { ScreenStub } from '@/components/screen-stub'

export default function ReviewPage() {
  return (
    <ScreenStub
      title="Review queue"
      badge="Phase 1B · Day 4"
      purpose="The throughput screen. Keyboard-first split pane over document and extraction."
      notes="Full keyboard contract (§7) lands with the extraction pipeline and lib/jobs/*."
    />
  )
}
