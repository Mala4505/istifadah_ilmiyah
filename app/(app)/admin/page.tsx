import { ScreenStub } from '@/components/screen-stub'

export default function AdminPage() {
  return (
    <ScreenStub
      title="Admin"
      badge="Phase 1A · Day 4"
      purpose="Users and roles; department assignment; budget-head → head mapping; vendor merge; zone/head master; Hub-status lifecycle rules."
      notes="Admin-only. The merge screen deferred from earlier planning lives here."
    />
  )
}
